Agree on a data structure for the map (sad char *[])

-- INIT
	- PARSING:
		- pars float, check doors
		- base fov ?
		- view angle ?
		- check if doors are opened in 2 dir at least (not forced to be on opposite side ?)?

-- DOORS:
	-- hash map for doors (swag)
	-- animation  !!! (from bot to top ?) (from middle to sides ?) (left <-> right ?) 
	-- update floodfill
	-- update raycasting (do a 2nd check for doors ?) (use same fct, but give a ptr to fct in arg?)
	-- update coll for a closed door
	-- update minimap (colors for cvlosed anbd open doors ?)

-- HOOKS
	-- open / close doors
	-- open settings

-- GAME
	- Fixed tick rate ?

-- PLAYER
	- variable speed ?

-- MINIMAP
	- squre player ?
	- plane view ?
	- add dirrection arrow
	- add doors

-- SETTINGS:
	--	PUT PAUSESCREEN WHEN LOSE FOCUS
	- fov
	- mouse sensibility
	- antialiasing ?
	- mmap size ?

-- RENDER
	- fix fisheye
	- make a better draw col_call (use avr color when its squished or extended)

- Sort files in hooks, create a game folder

-- Sprite :(

-- BONUS
	FIX PARSING FOR BNS-> add elemets in struct pars, fix init

	-- FPS counter
	-- Bousole ? (Barre avec N E S W en haut)
