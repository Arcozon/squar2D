Agree on a data structure for the map (sad char *[])

-- INIT
	- PARSING:
		- pars float, check doors
		- base fov ?
		- view angle ?
		- check if doors are opened in 2 dir at least (not forced to be on opposite side ?)?



-- HOOKS
	-- open settings

-- DOORS
	-- update coll for a closed door
	-- update minimap (colors for cvlosed anbd open doors ?)
	- add a door side
	- store the oriantation in s_door (ver / hor)
	- raycsting check		-- make the a rectangle (gl bro)
	- colision check		-- make door stop

-- GAME
	- Fixed tick rate ?

-- PLAYER
	- variable speed ?

-- MINIMAP
	- squre player ?
	- plane view ?
	- add dirrection arrow
	- add doors !!

-- SETTINGS:
	--	PUT PAUSESCREEN WHEN LOSE FOCUS
	- fov
	- mouse sensibility
	- antialiasing ?
	- mmap size ?

-- RENDER
	- fix fisheye ?
	- make a better draw col_call (use avr color when its squished or extended)

- Sort files in hooks, create a game folder

-- Sprite :(

-- BONUS
	-- FPS counter
	-- Bousole ? (Barre avec N E S W en haut)
